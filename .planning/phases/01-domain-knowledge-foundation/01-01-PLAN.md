---
phase: 01-domain-knowledge-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rules/aso-domain.md
autonomous: true
requirements:
  - FOUN-01
  - FOUN-02
  - FOUN-03

must_haves:
  truths:
    - "A rules file exists at rules/aso-domain.md containing verified character limits for both iOS and Google Play with source URLs and verification dates"
    - "Platform auto-detection logic is documented: given any Apple URL, Google Play URL, numeric ID, or bundle ID, the correct platform is identified"
    - "An explicit anti-fabrication directive prohibits inventing search volume data, and the OBSERVED vs ESTIMATED labeling convention is defined with clear examples"
    - "Scoring rubric defines three categories (Metadata 50%, Visibility 25%, Conversion 25%) with per-factor weights and scoring anchors"
    - "The file is between 200 and 350 lines to minimize context budget impact"
  artifacts:
    - path: "rules/aso-domain.md"
      provides: "ASO domain knowledge rules file — single source of truth for all subsequent commands"
      min_lines: 200
      contains: "ASO Domain Knowledge"
  key_links:
    - from: "rules/aso-domain.md"
      to: "All future /aso:* skill files"
      via: "Claude Code auto-discovery from ~/.claude/rules/ after install.sh runs"
      pattern: "Character Limits|Platform Detection|Scoring Rubric|Data Integrity"
---

<objective>
Create the ASO domain knowledge rules file that serves as the single source of truth for all subsequent /aso:* commands.

Purpose: Every future skill (audit, keywords, optimize) needs verified character limits, platform detection logic, scoring rubrics, ranking factors, and data labeling conventions. Encoding this once in a rules file prevents duplication and ensures consistency. This is the foundation the entire toolkit builds on.

Output: `rules/aso-domain.md` — a 200-350 line markdown file containing five sections of verified ASO reference data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-domain-knowledge-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ASO domain knowledge rules file</name>
  <files>rules/aso-domain.md</files>

  <read_first>
    .planning/phases/01-domain-knowledge-foundation/01-RESEARCH.md (contains verified character limits, platform detection patterns, scoring rubric weights, data integrity directives, and all source URLs — use Code Examples section as primary reference for exact content)
    CLAUDE.md (project constraints — zero infrastructure, no hallucinated data, file size constraint)
  </read_first>

  <action>
Create the directory `rules/` and the file `rules/aso-domain.md`. The file must contain exactly these sections in this order. Use the verified data from 01-RESEARCH.md Code Examples section as the primary source — do not invent new values.

**Section 1: Data Integrity (MUST be first section)**
- H2 heading: `## Data Integrity`
- H3: `### CRITICAL: No Fabricated Data`
- Explicit directive: "NEVER invent, estimate, or hallucinate:" followed by bullet list of prohibited items: search volume numbers, keyword difficulty scores, ranking positions, download counts, conversion rates
- Fallback statement: "If data cannot be retrieved from a verifiable web source, state: 'Data unavailable — use [Sensor Tower / AppTweak / App Store Connect Analytics] for verified metrics.'"
- H3: `### Data Labeling Convention`
- Table with two rows: OBSERVED (retrieved from live, verifiable source) and ESTIMATED (inferred by Claude based on analysis)
- Final directive: "Never present ESTIMATED data without the label. Never omit the label."

**Section 2: Platform Character Limits**
- H2 heading: `## Platform Character Limits`
- H3: `### iOS (App Store Connect)` with HTML comment containing source URL `developer.apple.com/app-store/product-page/` and verification date `2026-04-02`
- Table with these exact rows:
  - App Name (Title) | 30 chars | YES (highest weight) | Most critical metadata field
  - Subtitle | 30 chars | YES | Visible in search results
  - Keyword Field | 100 chars | YES (hidden) | Comma-separated, no spaces after commas, no duplication with title/subtitle
  - Description | 4,000 chars | NO | Write for conversion, not algorithm. Not indexed by Apple.
  - Promotional Text | 170 chars | NO | Updatable without new app version
  - What's New | 4,000 chars | NO | Release notes
- H3: `### Google Play Console` with HTML comment containing source URL `support.google.com/googleplay/android-developer/answer/9898842`, verification date `2026-04-02`, and note about title limit reduction from 50 to 30 on 2021-09-29
- Table with these exact rows:
  - Title | 30 chars | YES (highest weight) | Reduced from 50 chars in Sept 2021
  - Short Description | 80 chars | YES | Visible by default on listing
  - Full Description | 4,000 chars | YES | Unlike iOS, this IS indexed for search
  - What's New | 500 chars | NO | Per-release update notes
- IMPORTANT: Google Play title is 30 chars NOT 50. The PROJECT.md contains the stale 50-char figure. The rules file corrects this per research findings.
- H3: `### iOS Keyword Field Rules` — comma-separated, no spaces after commas, do not repeat words already in title or subtitle, prefer singular forms, 100 characters total including commas, do not use competitor brand names
- H3: `### Google Play Prohibited Title Words` — brief list: "Best", "#1", "Free", "Top", emojis, misleading performance claims. Note: policy violation risk.

**Section 3: Platform Detection**
- H2 heading: `## Platform Detection`
- Brief intro: "Given user input, detect the platform using this priority order:"
- H3: `### URL Patterns` — table with columns Pattern | Platform | Example
  - `apps.apple.com/*` | iOS | https://apps.apple.com/us/app/myapp/id123456789
  - `apps.apple.com/{country}/app/*` | iOS | https://apps.apple.com/de/app/myapp/id123456789
  - `itunes.apple.com/*` | iOS (legacy) | https://itunes.apple.com/app/id123456789
  - `play.google.com/store/apps/details?id=*` | Android | https://play.google.com/store/apps/details?id=com.example.app
- H3: `### ID Patterns` — table with columns Pattern | Platform | Example
  - Pure digits (typically 9-10 chars) | iOS | 284882215
  - Reverse-domain notation (contains dots) | Android | com.facebook.Facebook
- H3: `### Ambiguous Input` — table: App name (plain text) -> ask user or search both; Unknown URL -> ask user

**Section 4: ASO Scoring Rubric**
- H2 heading: `## ASO Scoring Rubric`
- Brief intro: "Score: 0-100 scale. Each factor scored 0-10, multiplied by weight, summed."
- H3: `### Category Weights` — table:
  - Metadata | 50% | Title (20%), Subtitle/Short Desc (15%), Keywords/Description (15%)
  - Visibility | 25% | Ratings & Reviews (15%), Ranking Signals (10%)
  - Conversion | 25% | Screenshots (15%), Icon (5%), Description Quality (5%)
- H3: `### Platform-Specific Adjustments` — bullet list:
  - iOS: Keywords factor = Keyword Field optimization (15%)
  - Android: Keywords factor = Full Description keyword density (15%)
  - iOS: Description Quality = conversion copywriting (not indexed)
  - Android: Description Quality = keyword integration + readability (indexed)
- H3: `### Letter Grades` — table mapping score ranges to grades: A+ (95-100), A (90-94), B+ (85-89), B (80-84), C+ (75-79), C (70-74), D (60-69), F (below 60)
- H3: `### Scoring Anchors` — brief anchors for key factors so Claude produces differentiated scores:
  - Title (9-10): Primary keyword first, within char limit, readable brand integration. (5-7): Keywords present but not front-loaded, or slight readability issues. (0-3): No target keywords, exceeds limit, or keyword-stuffed gibberish.
  - Subtitle/Short Desc (9-10): Unique keywords not in title, compelling value prop, within limit. (5-7): Some keyword overlap with title, generic phrasing. (0-3): Duplicates title words, empty, or exceeds limit.
  - Keyword Field/Description (9-10): Maximizes unique coverage, follows formatting rules, zero waste. (5-7): Some duplication or unused capacity. (0-3): Major formatting errors, heavy duplication, mostly wasted space.

**Section 5: ASO Ranking Factors**
- H2 heading: `## ASO Ranking Factors`
- Table with columns: Factor | iOS Weight | Android Weight | Notes
  - Title keywords | Very High | Very High | Most important metadata field on both platforms
  - Subtitle (iOS) / Short Desc (Android) | High | High | Secondary metadata signal
  - Keyword field (iOS only) | High | N/A | Hidden indexed field, iOS exclusive
  - Full description keywords | None | Medium | iOS does NOT index description; Google Play DOES
  - Download volume & velocity | High | High | Installs relative to category peers
  - Ratings (4.0+ threshold) | High | High | Below 4.0 significantly hurts conversion and ranking
  - Review count & recency | Medium | Medium | Fresh reviews signal active user base
  - Retention & engagement | Medium-High | Medium-High | Growing signal since 2024-2025
  - Update frequency | Medium | Medium | Signals active maintenance
  - Crash rate / stability | Medium | Medium | Quality signal, especially on Android
- Brief closing note: "Ranking factor weights are not published by Apple or Google. The above reflects industry consensus as of 2026."

**Section 6: Terminology**
- H2 heading: `## Terminology`
- Compact definition list of key ASO terms used across commands: ASO, Keyword Field, Short Description, Organic Installs, Conversion Rate (store listing), Keyword Density, Long-Tail Keywords, Search Visibility, Category Ranking, Custom Product Pages (CPP)

**File-level requirements:**
- H1 heading: `# ASO Domain Knowledge`
- Total file length: 200-350 lines
- No workflow instructions (no "Step 1:", "When the user asks", "Use WebSearch to")
- No code blocks with tool invocations
- Pure reference data: facts, tables, definitions, directives
  </action>

  <verify>
    <automated>wc -l rules/aso-domain.md | awk '{if ($1 >= 200 && $1 <= 350) print "PASS: " $1 " lines"; else print "FAIL: " $1 " lines (must be 200-350)"}'</automated>
  </verify>

  <acceptance_criteria>
- rules/aso-domain.md exists and is not empty
- Line 1 contains `# ASO Domain Knowledge`
- File contains `## Data Integrity` as the first H2 section
- File contains the string `NEVER invent, estimate, or hallucinate`
- File contains a table row with `OBSERVED` and a table row with `ESTIMATED`
- File contains `## Platform Character Limits`
- File contains `| App Name (Title) | 30 chars |` (iOS title)
- File contains `| Title | 30 chars |` (Google Play title — NOT 50)
- File does NOT contain the string `50 chars` for Google Play title
- File contains `| Keyword Field | 100 chars |`
- File contains `| Short Description | 80 chars |`
- File contains `## Platform Detection`
- File contains `apps.apple.com` and `play.google.com` in the detection tables
- File contains `284882215` as an iOS numeric ID example
- File contains `com.facebook.Facebook` as an Android bundle ID example
- File contains `## ASO Scoring Rubric`
- File contains `Metadata | 50%` and `Visibility | 25%` and `Conversion | 25%`
- File contains `## ASO Ranking Factors`
- File contains `## Terminology`
- File is between 200 and 350 lines: `wc -l rules/aso-domain.md` returns a number in that range
- File does NOT contain "Step 1:" or "When the user asks" or "Use WebSearch"
  </acceptance_criteria>

  <done>The rules file exists at rules/aso-domain.md with all six sections containing verified ASO domain knowledge. Character limits use the correct 30-char Google Play title (not the stale 50-char figure). Anti-fabrication directive is the first section. Scoring rubric has differentiated anchors. File is within the 200-350 line budget.</done>
</task>

<task type="auto">
  <name>Task 2: Validate rules file against all phase requirements</name>
  <files>rules/aso-domain.md</files>

  <read_first>
    rules/aso-domain.md (the file just created — verify its actual content)
    .planning/phases/01-domain-knowledge-foundation/01-RESEARCH.md (Validation Architecture section — contains the grep commands and requirement-to-test mapping)
  </read_first>

  <action>
Run a comprehensive validation of rules/aso-domain.md against all three phase requirements. Execute these checks and fix any failures found:

**FOUN-01 validation (domain knowledge encoding):**
```bash
grep -c "iOS\|Google Play\|Title\|Subtitle\|Keyword" rules/aso-domain.md
```
Expected: count >= 30 (the file should reference these terms extensively).

Also verify specific content:
- `grep "App Name (Title) | 30 chars" rules/aso-domain.md` — iOS title limit present
- `grep "Title | 30 chars" rules/aso-domain.md` — Google Play title limit present (30, not 50)
- `grep "Keyword Field | 100 chars" rules/aso-domain.md` — iOS keyword field limit present
- `grep "Short Description | 80 chars" rules/aso-domain.md` — Google Play short desc limit present
- `grep "Metadata | 50%" rules/aso-domain.md` — scoring rubric weights present
- `grep "Visibility | 25%" rules/aso-domain.md` — scoring rubric weights present
- `grep "Conversion | 25%" rules/aso-domain.md` — scoring rubric weights present
- `grep "Title (20%)" rules/aso-domain.md` — per-factor weights present

**FOUN-02 validation (platform auto-detection):**
```bash
grep -c "apps.apple.com\|play.google.com\|numeric\|bundle\|Reverse-domain" rules/aso-domain.md
```
Expected: count >= 8.

Also verify:
- `grep "284882215" rules/aso-domain.md` — iOS numeric ID example present
- `grep "com.facebook.Facebook" rules/aso-domain.md` — Android bundle ID example present
- `grep "Ambiguous Input" rules/aso-domain.md` — ambiguous case handling present

**FOUN-03 validation (data labeling):**
```bash
grep -c "OBSERVED\|ESTIMATED\|fabricat\|hallucin\|NEVER" rules/aso-domain.md
```
Expected: count >= 8.

Also verify:
- `grep "NEVER invent" rules/aso-domain.md` — anti-fabrication directive present
- `grep "OBSERVED" rules/aso-domain.md` — OBSERVED label defined
- `grep "ESTIMATED" rules/aso-domain.md` — ESTIMATED label defined
- `grep "Data unavailable" rules/aso-domain.md` — fallback guidance present

**File health checks:**
- `wc -l rules/aso-domain.md` — between 200 and 350 lines
- `head -1 rules/aso-domain.md` — first line is `# ASO Domain Knowledge`
- `grep -c "Step 1:\|When the user asks\|Use WebSearch" rules/aso-domain.md` — must be 0 (no workflow instructions)
- `grep "50 chars" rules/aso-domain.md` — must NOT appear in a Google Play title context (check any matches are not Google Play title)

If any check fails, fix the rules file to pass all validations. Do not proceed until every check passes.
  </action>

  <verify>
    <automated>grep -q "NEVER invent" rules/aso-domain.md && grep -q "Metadata | 50%" rules/aso-domain.md && grep -q "apps.apple.com" rules/aso-domain.md && grep -q "OBSERVED" rules/aso-domain.md && echo "PASS: All requirement checks passed" || echo "FAIL: One or more requirement checks failed"</automated>
  </verify>

  <acceptance_criteria>
- `grep -c "iOS\|Google Play\|Title\|Subtitle\|Keyword" rules/aso-domain.md` returns >= 30
- `grep -c "apps.apple.com\|play.google.com\|numeric\|bundle\|Reverse-domain" rules/aso-domain.md` returns >= 8
- `grep -c "OBSERVED\|ESTIMATED\|fabricat\|hallucin\|NEVER" rules/aso-domain.md` returns >= 8
- `grep -c "Step 1:\|When the user asks\|Use WebSearch" rules/aso-domain.md` returns 0
- `wc -l rules/aso-domain.md` returns a value between 200 and 350
- All FOUN-01, FOUN-02, FOUN-03 grep checks from the action section pass
  </acceptance_criteria>

  <done>All three phase requirements (FOUN-01, FOUN-02, FOUN-03) pass automated validation. The rules file contains no workflow instructions, stays within the line budget, and encodes correct data (including the corrected 30-char Google Play title limit).</done>
</task>

</tasks>

<verification>
Phase-level verification after all tasks complete:

1. **File exists and has correct structure:**
   - `test -f rules/aso-domain.md && echo "EXISTS" || echo "MISSING"`
   - `head -1 rules/aso-domain.md` outputs `# ASO Domain Knowledge`

2. **Line budget respected:**
   - `wc -l rules/aso-domain.md` returns 200-350

3. **FOUN-01 — Domain knowledge encoded:**
   - Character limits for both platforms present with source URLs in HTML comments
   - Scoring rubric with three-category weights (50/25/25) present
   - Ranking factors table with per-platform weights present
   - Terminology definitions present

4. **FOUN-02 — Platform detection documented:**
   - URL patterns for apps.apple.com, itunes.apple.com, play.google.com present
   - ID patterns for numeric (iOS) and reverse-domain (Android) present
   - Ambiguous input handling present

5. **FOUN-03 — Data labeling convention defined:**
   - Anti-fabrication directive is the FIRST section in the file
   - OBSERVED and ESTIMATED labels defined with clear definitions
   - Prohibited data types listed (search volume, difficulty scores, ranking positions, download counts, conversion rates)

6. **No stale data:**
   - Google Play title limit is 30 chars, not 50
   - iOS keyword field is 100 chars (not bytes)

7. **No workflow contamination:**
   - File contains zero instances of "Step 1:", "When the user asks", "Use WebSearch"
</verification>

<success_criteria>
- rules/aso-domain.md exists with 200-350 lines of verified ASO reference data
- All three requirements (FOUN-01, FOUN-02, FOUN-03) pass grep-based validation
- Google Play title limit correctly encoded as 30 characters
- Anti-fabrication directive is first H2 section
- No workflow instructions in the file
- Scoring rubric has differentiated anchors (high/medium/low descriptions)
</success_criteria>

<output>
After completion, create `.planning/phases/01-domain-knowledge-foundation/01-01-SUMMARY.md`
</output>
