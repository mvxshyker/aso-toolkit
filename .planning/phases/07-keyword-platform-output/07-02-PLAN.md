---
phase: 07-keyword-platform-output
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified: [commands/aso/keywords.md]
autonomous: true
requirements: [KWRD-09]

must_haves:
  truths:
    - "Running /aso:keywords writes a structured .md report file to the user's working directory"
    - "An inline summary is printed in the conversation showing top keywords and platform output"
    - "The report filename follows the same slug convention as audit reports"
  artifacts:
    - path: "commands/aso/keywords.md"
      provides: "Report Output section with Write tool instructions and filename convention"
      contains: "## Report Output"
    - path: "commands/aso/keywords.md"
      provides: "Updated Analysis Summary referencing saved report"
      contains: "Report saved"
  key_links:
    - from: "commands/aso/keywords.md Report Output section"
      to: "commands/aso/audit.md Report Output section"
      via: "same filename slug convention and Write tool pattern"
      pattern: "app_name_slug.*keyword.*YYYY-MM-DD"
    - from: "commands/aso/keywords.md Report Output section"
      to: "commands/aso/keywords.md Analysis Summary section"
      via: "Analysis Summary references the saved report file"
      pattern: "Report saved"
---

<objective>
Add report file output to the /aso:keywords command, matching the pattern established by /aso:audit.

Purpose: Users need a persistent .md report file for keyword research results, not just in-conversation output. This completes the keyword command's output pipeline.

Output: Report Output section added to commands/aso/keywords.md with filename convention, contents list, and post-save confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@commands/aso/keywords.md
@commands/aso/audit.md
@.planning/phases/07-keyword-platform-output/07-01-SUMMARY.md
</context>

<interfaces>
<!-- Report Output pattern from audit.md (lines 563-611) to replicate -->

Filename convention from audit.md:
  {app_name_slug}-aso-audit-{YYYY-MM-DD}.md
  Slug rules: lowercase, spaces to hyphens, strip non-alphanumeric (except hyphens), collapse consecutive hyphens

Post-save confirmation from audit.md:
  "Report saved: ./{filename}"

Write tool usage from audit.md:
  Use the Write tool to save the file to the current working directory.

<!-- Current keywords.md section order (after Plan 01 adds content) -->
Expected file structure after Plan 01:
- YAML frontmatter
- Input Resolution
- Keyword Expansion
- Relevance Scoring
- User-Provided Data Integration
- Popularity Signals
- Intent Grouping
- iOS Keyword Field String (NEW from Plan 01)
- Google Play Keyword Integration (NEW from Plan 01)
- Prioritized Focus Keywords (NEW from Plan 01)
- Analysis Summary
- Data Labeling Convention

Insertion point for Report Output: between "Analysis Summary" and "Data Labeling Convention" (end of file).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Report Output section and update Analysis Summary references</name>
  <files>commands/aso/keywords.md</files>
  <read_first>commands/aso/keywords.md, commands/aso/audit.md</read_first>
  <action>
Insert a new "## Report Output" section into commands/aso/keywords.md between the Analysis Summary section and the Data Labeling Convention section (the last two sections in the file). Place a `---` separator before it.

Follow the same pattern established in commands/aso/audit.md Report Output section (lines 563-611).

**Section: "## Report Output" (KWRD-09)**

After completing the analysis and printing the Analysis Summary above, save the full keyword research as a Markdown report file.

Subsections:

1. **Filename Convention** -- Build the filename from the app name and current date:

   `{app_name_slug}-keyword-research-{YYYY-MM-DD}.md`

   Where:
   - `{app_name_slug}` is the app name lowercased with spaces replaced by hyphens, non-alphanumeric characters (except hyphens) removed, and consecutive hyphens collapsed to one (same slug rules as audit.md -- e.g., "My App: Pro Edition!" becomes `my-app-pro-edition`)
   - `{YYYY-MM-DD}` is today's date

   Example: `calm-keyword-research-2026-04-02.md`

2. **Report Contents** -- Use the **Write tool** to save the file to the current working directory. The report must contain ALL of the following sections from this keyword research, in order:

   1. **Header** -- App name, platform, date, seed count, total keyword count
   2. **Quick Stats Table** -- The same table from Analysis Summary
   3. **Relevance Scoring Table** -- Full scored keyword table
   4. **Intent Grouping** -- All intent group tables with distribution summary
   5. **iOS Keyword Field String** (iOS only) -- Generated keyword string with coverage report
   6. **Google Play Keyword Integration** (Android only) -- Priority keyword table with placement guidance
   7. **Prioritized Focus Keywords** -- Top 10-15 focus keyword table with strategic summary
   8. **Footer** -- "Generated by /aso:keywords on {YYYY-MM-DD}"

   Format the header as:

   ```markdown
   # Keyword Research: {App Name}

   **Platform:** {iOS / Android}
   **Date:** {YYYY-MM-DD}
   **Seed Keywords:** {N} | **Total Candidates:** {N} | **High Relevance (7+):** {N}
   ```

3. **Post-Save Confirmation** -- After writing the file, print:

   `Report saved: ./{filename}`

**Also update the Analysis Summary section:**

In the Analysis Summary "Next Steps" subsection, add a new first bullet:
- NEW first bullet: `"Full keyword research report saved to your working directory (see Report Output below)."`
- Keep the existing bullets about `/aso:optimize` and `/aso:audit` as-is.
  </action>
  <verify>
    <automated>grep -c "## Report Output" commands/aso/keywords.md && grep -c "keyword-research-" commands/aso/keywords.md && grep -c "Report saved" commands/aso/keywords.md && grep -c "Generated by /aso:keywords" commands/aso/keywords.md</automated>
  </verify>
  <acceptance_criteria>
- grep "## Report Output" commands/aso/keywords.md returns 1 match
- grep "keyword-research-" commands/aso/keywords.md returns at least 1 match (filename convention)
- grep "Report saved" commands/aso/keywords.md returns at least 1 match (post-save confirmation)
- grep "Generated by /aso:keywords" commands/aso/keywords.md returns 1 match (footer template)
- grep "Write tool" commands/aso/keywords.md returns at least 1 match (file output mechanism)
- grep "Full keyword research report saved" commands/aso/keywords.md returns 1 match (updated Next Steps)
- The "## Report Output" section appears AFTER "## Analysis Summary" in the file
- The "## Report Output" section appears BEFORE "## Data Labeling Convention" in the file
- File line count is between 560 and 720 lines (Plan 01 brought it to ~500-650, this adds ~60-80 lines)
  </acceptance_criteria>
  <done>
- Report Output section specifies filename convention (keyword-research slug), report contents (8 sections), Write tool usage, and post-save confirmation matching the audit.md pattern
- Analysis Summary Next Steps updated with reference to saved report file
- The /aso:keywords command now writes a persistent .md report AND prints an inline summary
  </done>
</task>

</tasks>

<verification>
Phase 7 Plan 02 verification:

1. `grep "## Report Output" commands/aso/keywords.md` -- section exists
2. `grep "keyword-research-" commands/aso/keywords.md` -- filename convention present
3. `grep "Write tool" commands/aso/keywords.md` -- file output mechanism specified
4. `grep "Report saved" commands/aso/keywords.md` -- post-save confirmation present
5. `grep "Generated by /aso:keywords" commands/aso/keywords.md` -- footer template present
6. `grep "Full keyword research report saved" commands/aso/keywords.md` -- Next Steps updated
7. `wc -l commands/aso/keywords.md` -- between 560-720 lines
</verification>

<success_criteria>
The /aso:keywords command now saves a complete keyword research report as a .md file to the working directory AND prints an inline summary, matching the output pattern established by /aso:audit.
</success_criteria>

<output>
After completion, create `.planning/phases/07-keyword-platform-output/07-02-SUMMARY.md`
</output>
